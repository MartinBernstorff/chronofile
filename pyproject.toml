[project]
name = "rescuetime-to-gcal"
version = "0.1.0"
description = "Add your description here"
authors = [
    { name = "Martin Bernstorff", email = "martinbernstorff@gmail.com" }
]
dependencies = [
    "gcsa>=2.2.0",
    "pandas>=2.2.2",
    "pydantic>=2.7.1",
    "wasabi>=1.1.2",
]
readme = "README.md"
requires-python = ">= 3.8"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.rye]
managed = true
dev-dependencies = []

[tool.hatch.metadata]
allow-direct-references = true

[tool.hatch.build.targets.wheel]
packages = ["src/rescuetime_to_gcal"]
